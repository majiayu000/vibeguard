id: rs-03-unwrap
language: Rust
severity: warning
rule:
  pattern: $VAL.unwrap()
message: ".unwrap() 在生产代码中。修复：使用 ? 操作符或 unwrap_or* 变体"
---
id: rs-03-expect
language: Rust
severity: warning
rule:
  pattern: $VAL.expect($MSG)
message: ".expect() 在生产代码中。修复：使用 ? 操作符传播错误"
