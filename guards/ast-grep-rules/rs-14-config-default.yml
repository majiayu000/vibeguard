id: rs-14-config-default
language: Rust
severity: warning
rule:
  any:
    - pattern: $T::default()
    - pattern: $T::<$ARGS>::default()
message: "Config 类型通过 Default::default() 初始化。若 Config 有 load() 方法，应使用 Config::load() 加载配置"
