{
  "name": "vibeguard-mcp-server",
  "version": "1.0.0",
  "description": "VibeGuard MCP Server - AI anti-hallucination guards as tools",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run build --silent && node --test tests/detector-and-guards.test.mjs tests/integration/test_mcp_server.mjs",
    "test:unit": "npm run build --silent && node --test tests/detector-and-guards.test.mjs",
    "test:integration": "npm run build --silent && node --test tests/integration/test_mcp_server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
