[package]
name = "vg-helper"
version = "0.1.0"
edition = "2024"
description = "VibeGuard hook helper — fast JSON/JSONL ops replacing Python subprocesses"

[dependencies]
serde_json = "1"
regex = "1"

[profile.release]
opt-level = "z"
lto = true
strip = true
