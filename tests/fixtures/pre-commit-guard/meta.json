{
  "hook": "pre-commit-guard.sh",
  "hook_type": "git-hook",
  "input_format": "commit_script",
  "cases": {
    "tp/go-discard-error.sh": {
      "rule": "GO-01",
      "expect": "block",
      "description": "Go guard interception _ = discard error submissions"
    },
    "fp/rust-timeout-fallback.sh": {
      "rule": "BUILD",
      "expect": "pass",
      "description": "Return to execution when timeout is unavailable, and no false reports of build failure"
    }
  }
}